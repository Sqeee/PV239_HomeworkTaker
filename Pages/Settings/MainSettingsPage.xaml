<Page
    x:Class="HomeworkTaker.Pages.Settings.MainSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HomeworkTaker.Pages.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="Manage your subjects" Click="onSubjectsManagementBtnClick" Background="White" FontWeight="SemiBold" Margin="10, 10, 10, 5" BorderBrush="Black" BorderThickness="1"/>
        <Button Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="Manage your schedule" Click="onScheduleManagementBtnClick" Background="White" FontWeight="SemiBold" Margin="10, 5, 10, 10" BorderBrush="Black" BorderThickness="1"/>
    </Background>
</Page>
