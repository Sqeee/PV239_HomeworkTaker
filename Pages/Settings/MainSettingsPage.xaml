<Page
    x:Class="HomeworkTaker.Pages.Settings.MainSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HomeworkTaker.Pages.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border BorderThickness="1" BorderBrush="Black" Grid.Row="0">
            <Button VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="Subjects management" Click="onSubjectsManagementBtnClick"/>
        </Border>
        <Border BorderThickness="1" BorderBrush="Black" Grid.Row="1">
            <Button VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="Schedule management" Click="onScheduleManagementBtnClick"/>
        </Border>
    </Grid>
</Page>
